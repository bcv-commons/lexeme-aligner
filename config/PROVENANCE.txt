lexeme-spine.db — LEXEME-anchored backbone (the current spine; spine_words table)  [ALIGNER_SPINE_DB]
  from bcv-commons/shoresh MACULA export, 2026-07, CC-BY. 607,255 words (finer MACULA tokenization).
  anchor: lexeme = lang:augmented-strong (e.g. hbo:0871a) ; strong = augment-stripped rollup
    (Hebrew equivalence-canonicalized for spine.db parity). Adds gloss + role columns.
  sources: Clear-Bible/macula-greek@main (Nestle1904) + macula-hebrew@main (WLC)
  pin: spine_meta.macula_spine_sha256 = 9a1f8bd524c2783d20034bfdcbcffc2fadb78e74e9c1b6cd6c84bc9e758b1e71
    — UNCHANGED across the 2026-09-23 deliveries below: this tracks the underlying MACULA TSV *source*
    data, which didn't change; only new DERIVED columns/tables were added on top of it. The actual
    DISTRIBUTED FILE hash (pipeline/lexeme-spine.db itself, verified via sha256sum) is
    4bfd0cdd0a4eed93c5f14fe073f7c96f25ccd07073dc4888c6f9c4fc4c882125 as of the second 2026-09-23
    delivery below — use THIS one to identify the exact file, not the source-tracking spine_meta value.
  2026-09-23 deliveries (internal-docs/bcv-query-wishlist.md #3/#3b/#10), both verified against real
  data, not taken on faith:
    (1) `head_idx` (the row's clause verb's idx, same verse; NULL if the row IS that verb — a stated
        simplification, not a full dependency parse) and `phrase_role` (v/s/o/o2/p/pp/adv) — built from
        MACULA Hebrew's lowfat XML (WLC/lowfat/, a separate treebank product from the flat TSV, not
        previously ingested); 266,393/415,872 OT tokens covered. Greek NOT covered (no lowfat
        distribution for macula-greek). Confirmed populated on JOS 1:14 directly.
        Also `construct_group` (a second, independent construct-chain id from the lowfat tree's own
        `rule="NPofNP"` grouping, 115,925 tokens) — NOTE bcv-query's own reply says construct-chain
        grouping was ALREADY fully answerable before this from `state='construct'` + `phrase_id` +
        `rela='rec'` (shipped 2026-07); this column is a cross-check, not a new capability.
    (2) `spine_assimilated_articles` — a NEW COMPANION TABLE (book, chapter, verse, after_idx, lemma,
        gloss, strong), 6,435 rows, fulfilling wishlist #10 (the phonologically-assimilated Hebrew
        article — a preposition+noun that in the written text hides the definite article as a vowel-
        pointing change, no separate consonant). Deliberately NOT spliced into spine_words as a new row
        (book,chapter,verse,idx) is a published, pinned join key and splicing would shift idx for every
        later token in an affected verse — a companion table pointing at `after_idx` avoids that.
        Confirmed directly: JOS 1:14 -> (after_idx=8, lemma=הַ, gloss=the, strong=1886a), exactly the
        preposition token (idx 8, "בָּ" "in") independently identified diagnosing the Joshua comparison
        artifact's reconciliation gap (see internal-docs/llm-align-experiment-plan.md).
  NONE of head_idx/phrase_role/construct_group/spine_assimilated_articles are wired into HebToken/
  hebrew_source.py yet — same "forward-compatible, zero-code-change drop-in whenever it lands" pattern
  as `is_superscription`/`sense` before them; wiring (and, for spine_assimilated_articles specifically,
  fixing the Joshua comparison artifact's "no reliable comparison" fallback verses now that the missing
  token is actually available) is a separate, not-yet-started follow-up.
  Also surfaced, unused: `macula-spine.db`'s own `frames` table (verb_key/role/arg_key, PropBank-style
  A0/A1/A2 argument labels, 109,540 Hebrew rows, CC-BY — NOT MARBLE-blocked, distinct from the blocked
  sense-gloss data) — ingested by their own parser but never surfaced through OUR spine; a candidate for
  future semantic-frame work, not something we asked for or need to ask for again.
  contract: docs/data-contracts.md §1. Provided out-of-band; gitignored (*.db).

rp2018-spine.db — ALTERNATE NT spine, Robinson-Pierpont 2018 Byzantine Textform  [ALIGNER_SPINE_DB]
  from bcv-commons/bcv-query (shoresh/macula/build_rp2018_spine.py), 2026-08-31. 140,143 words, NT only.
  source: Berean Bible group interlinear, majoritybible.com/msb_nt_tables.tsv
  pin: spine_meta.source_sha256 = f4f6d75c210b0df9af064e76133f11e334c7a3a47154fb0a9ac348c8a43c2710
  build sha256 (this artifact): 774a408f76f6974b5e9d77934dfe6bf81766f125ab6cdf4a4146382408e4c706
  license: Public domain — berean.bible/licensing.htm ("Berean Bible and Majority Bible texts placed
    into the public domain", 2023-04-30). The interlinear TABLE specifically was CLEARED with the
    publisher (operator-confirmed 2026-08-31) — an earlier note here flagged it as unresolved because
    berean.bible/terms.htm scopes its wording to the running texts; that caveat no longer applies.
  source_corpus value: grc-rp2018 (hebrew_source.spine_corpus, from spine_meta.edition)
  NOT Textus Receptus — spine_meta.edition_note records the direct check: 1 John 5:7-8 carries the SHORT
    reading. RP2018 also omits LUK 17:36 / ACT 8:37 / ACT 15:34 / ACT 24:7, which TR carries.
  FIDELITY vs lexeme-spine.db, all flagged in spine_meta, none silent:
    - lemma 0% populated (lexeme-spine.db: 100% on grc) -> breaks `benchmark --grain lexeme`
    - structured person/number/gender/case_/tense columns EXIST but are entirely NULL; the raw
      Robinson-Pierpont parsing code is kept verbatim in `morph` instead (which lexeme-spine.db leaves
      empty for Greek, so `morph` is a gain). Disables gapfill's number/gender agreement prior.
    - bare grc:<strong> anchors, homographs unsplit — NOT a regression: lexeme-spine.db already has
      0 augmented Greek lexemes (augmentation is Hebrew-only).
  use: --spine-db pipeline/rp2018-spine.db (full_chain/onboard) or ALIGNER_SPINE_DB.
  Provided out-of-band; gitignored (pipeline/*.db).

tr-textus-receptus-spine.db — ALTERNATE NT spine, Textus Receptus  [ALIGNER_SPINE_DB]
  from bcv-commons/bcv-query (shoresh/macula/build_tr_spine.py), 2026-08-31. 140,856 words, NT only.
  source: github.com/byztxt/greektext-textus-receptus — Robinson's TR with Strong's numbers
  pin: spine_meta.source_sha256 = c70e5b14765e22e98cd27736289c993cfb968a600a192fdc1c910cd1084c8270
  build sha256 (this artifact): 068a1730fbf0bd1cf9eab98292ed65f6eb3019fe108e09e35da0a46f3681c4cf
  license: Public domain — that repo's README ("Public Domain. Copy freely."), verified.
  source_corpus value: grc-tr (hebrew_source.spine_corpus, from spine_meta.edition)
  Carries the Comma Johanneum — spine_meta.comma_johanneum records the direct check; 1 John 5:7 is 22
    words here vs 5 in Nestle1904. This is the reason this spine exists: words present in TR but absent
    from Nestle1904 have NO anchor under the default spine and can never be aligned.
    CAVEAT proven in practice: a TR spine is necessary but NOT sufficient — aligning ind against it left
    17 of those 22 words unaligned, correctly, because every ingested Indonesian edition carries the
    SHORT reading. A TR-based TARGET edition is needed too (see config/textual_basis.json).
  FIDELITY vs lexeme-spine.db — same three as rp2018-spine.db, plus:
    - gloss always blank (rp2018 has 99.3% from the Berean source). No operational cost: spine `gloss`
      has zero consumers in the aligner, it is carried for reference only.
    - surface is unaccented/uncapitalized, transliterated from the source's ASCII form. Presentational
      only — eflomal keys on the anchor, never the surface.
  use: --spine-db pipeline/tr-textus-receptus-spine.db (full_chain/onboard) or ALIGNER_SPINE_DB.
  Provided out-of-band; gitignored (pipeline/*.db).

spine.db — legacy STEPBible bare-Strong's backbone (superseded by lexeme-spine.db; kept for parity checks)
  copied 2026-07 from bcv-query monorepo (shoresh/spine/spine.db)
  STEPBible-derived, CC-BY: uhb_tag v2.1.32 / ugnt_tag v0.34 ; 443,508 words
  stable reference — refresh only on a deliberate STEPBible upgrade
  (rebuild path: monorepo shoresh/spine/parse.py). Provided out-of-band; gitignored.

usj-ind/ — Indonesian target text (67 USJ books)
  derived 2026-07 from cdn.bibel.wiki PKF ind_C01 (ind_C01.CN8xM8h_.pkf)
  pipeline: PKF -> USFM (Proskomma, example/scripts/export_usfm.mjs) -> USJ (usfmtc.outUsj)
  license: Public Domain (verbal 2026-07, written confirmation pending) — experiment-only
  proper long-term home: bcv-commons/bibles recipe layer (pinned). Provided out-of-band; gitignored.

resources/strongs/attestations/*.parquet — GOLD word-alignments (10 languages)
  copied 2026-07 from bcv-query monorepo resources/strongs/attestations/
  source: Clear-Bible/Alignments (manual); base texts per-lang; CC-BY-SA-4.0 (see resources/strongs/README.md)
  langs: arb asm ben eng fra hau hin por rus spa  (NO Indonesian — Clear has no ind gold)
  schema: strong, lemma, surface, ref, target_id, source_id, method, source_corpus, base_text
  use: benchmark a produced lexeme-alignments/<lang>.tsv against attestations/<lang> (gold). ALIGNER_RESOURCES=data/resources.

usj-fra-lsg/ — French target text (66 USJ books, WHOLE BIBLE) — GOLD BENCHMARK + full alignment
  source: eBible.org fraLSG — Louis Segond 1910, PUBLIC DOMAIN (ebible.org/fraLSG)
  pipeline: eBible USFM zip -> usfmtc.outUsj -> USJ (aligner NN-BOOK.json numbering, OT+NT)
  WHY THIS EDITION: gold attestations/fra base_text=LSG — matching editions is required for a
    valid benchmark (else surfaces differ = measuring translation, not aligner quality).
  use: run the aligner on this -> produced lexeme-alignments/fra -> score vs strongs/attestations/fra (gold).
  NOTE 2026-07-09: fra was initially aligned NT-only; OT now aligned (96.1% cov) → lexeme-alignments/fra is
    whole-Bible (60,276 rows) + senses_attested/fra built. Clears shoresh's "publish OT for all langs" ask.

prior-pack/ — LANGUAGE-INDEPENDENT prior pack, PULLED from bcv-commons/prior-pack (HF dataset, CC-BY-4.0)
  one row per MACULA lexeme (14,720): keyness, lxx_greek/lxx_hebrew, senses inventory, neighbors,
  xling_confidence — feeds the recipes (R1 keyness-filter, R2 sense-surface, R3 gap-map, LXX NT-gap).
  PIN — prior_pack.parquet sha256: 9874ac4a5ecd88f8f6a1bc74c8bf5be408d78c67bbd98bd501ddeb882bdb13a8
  pull: huggingface_hub.snapshot_download(repo_id='bcv-commons/prior-pack', repo_type='dataset',
        local_dir='resources/prior-pack'). Bulk parquet gitignored; manifest.json + README committed as
  the pin. Rebuilt/re-pulled only on a shoresh component bump. Contract: internal-docs/aligner-handover.md.

resources/lexeme-alignments_gold_derived/ — {fra,por,hau}.tsv
  copied from monorepo resources/lexeme-alignments/ — CAUTION: these were DERIVED FROM the Clear gold
  ("fra/LSG via Clear-Bible/Alignments manual word alignment"), so scoring them vs the Clear gold
  is CIRCULAR (~100%). NOT aligner output. Use only as an upper-bound reference lexicon.

usj-hau-ohcb/ — Hausa target text (39 OT USJ books) — 2nd gold benchmark (typologically distant)
  source: eBible.org "hausa" (HAUBIB) = Hausa Contemporary Bible / OHCB, Biblica © 2009,2020
    (redistributable via eBible; experiment-only, gitignored — NOT public domain)
  pipeline: eBible USFM zip -> usfmtc.outUsj -> USJ (OT only). gold attestations/hau base_text=OHCB (match).
  why: Chadic/Afro-Asiatic, lower-resource — tests whether the method generalizes off European/Latin
    high-resource langs toward the Indonesian-like population (French alone = flattering case).

lexeme-spine.db — LEXEME-ANCHORED backbone (MACULA) — data-contract §1
  built 2026-07 in bcv-query monorepo: python -m macula.build_spine_words
  source: Clear-Bible macula-greek@main (Nestle1904) + macula-hebrew@main (WLC), CC-BY-4.0
  607,255 tokens; anchor lexeme=lang:augmented-strong (e.g. hbo:0871a); strong=augment-stripped rollup;
  is_content=class in {noun,verb,adj}; gloss on 99.1%; role on 46,782; morph absent (MACULA carries none).
  PIN — macula_spine_sha256 (input): a3dbc0bbe95e19e0b45b5dbc533ae4b82eb61d66f1f0e817c87bb8628ac5d0ac
        build sha256 (this artifact):  c954889d951e1a9082557dac4b7a31b4803705670371d604a0be6da913ad8b04
  use: set ALIGNER_SPINE_DB=data/lexeme-spine.db (has_lexeme=True). Finer tokenization than spine.db
       (prefixes split); re-pin + re-align on a deliberate MACULA bump.

  UPDATE 2026-07-09: rebuilt with per-occurrence `key` column (MACULA node id) — enables the
  BHSA↔MACULA sense bridge join (lexeme-spine.key = bridge.key). Additive; aligner SELECT unaffected.
  new build sha256: 8808bd09c4c880678495d3133fd20eb0c90e2ef51b512e43111ec45c45534127

  UPDATE 2026-07-09b: enriched with BHSA sense/sense_conf/sense_source via the bridge
  (macula.enrich_spine_senses) — 227,400 OT tokens carry homograph-precise sense. new sha256: ac0baa98af532e74759506e7ca630cbdd07121a4d846d0d9d453fd5984105ca7

  UPDATE 2026-07-09c: + BHSA `lex` + `stem` (binyan) columns alongside sense — lets the
  aligner emit shoresh canonical sense key (lex, stem, sense). new sha256: 752f77a7d0a77d0a09489ac1cac5bf89c762fae9b5d58d69db9899c45a25ad71

  UPDATE 2026-07-09d: FREED TO CC-BY — dropped BHSA `lex`; `stem` now = MACULA binyan (CC-BY,
  full names qal/piel/hiphil); sense# = in-house hebrew-context. Sense key is (lexeme, stem, sense),
  fully CC-BY → publishable. new sha256: dc353d340215339093f453d8765af1fabf8b444dcd917b4d73a3d056b22ebb31
  (MACULA input also refreshed this cycle → macula_spine_sha256 = a3dbc0bb…; header pin updated to match.)

  UPDATE 2026-07-25: + `is_superscription` (532 tokens, Psalm-title vocabulary — combines UHB's own
  \d-marked verse split with BSB-publishing heading data + an empirically-derived vocabulary for
  titles merged into verse 1; 115/116 titled psalms flagged in an earlier build, PSA 72 confirmed
  correctly flagged in the final one) + structured morphology (person/number/gender/case_/tense/
  voice/mood/degree/state, from BHSA/MACULA) + `phrase_id`/`function`/`rela` (BHSA phrase-level
  syntax — function = syntactic role e.g. Subj/Pred/Objc/Adju, 100% coverage across all 253,203
  Hebrew OT phrases; phrase_id groups tokens into the same construct-chain/multi-word syntactic
  unit; rela = fine relation within the phrase — NA=head, rec=construct-governed dependent,
  par=coordination; populated on 296,356 tokens, Hebrew/OT-only, no Greek layer). Built via a new
  resource, resources/occurrences/hbo_syntax.db, joined through the existing BHSA↔MACULA bridge —
  same mechanism `sense` already used. An earlier build in this cycle briefly DROPPED sense/
  sense_conf/sense_source (only ran build_spine_words, not build_bridge + enrich_spine_senses) —
  caught via our own PRAGMA-based auto-detection going False + a direct 0/2,741-token spot-check;
  fixed and rebuilt, verified restored: 227,400/607,255 tokens carry sense (37.4%), matching the
  documented baseline exactly. Verified independently on the final build (2026-07-25): sense
  coverage 227,400/607,255 (37.4%), phrase_id coverage 296,356/607,255 (48.8%), is_superscription
  532/607,255 — all match exactly. new sha256: 0498790c57948c10d7158bb49ccd2593ae4be0aa1319570a1fac45c259541918

  UPDATE 2026-09-23: + `head_idx`/`phrase_role` (from MACULA Hebrew's lowfat XML tree, WLC/lowfat/ —
  a richer sibling distribution to the flat TSV, same repo/CC-BY-4.0, not previously ingested;
  head_idx = idx of the row's clause verb, same verse, NULL if the row IS that verb; phrase_role =
  v/s/o/o2/p/pp/adv) + `construct_group` (second, independent source alongside the existing
  phrase_id+rela='rec' path, from the lowfat tree's own rule="NPofNP" grouping) — Hebrew/OT-only,
  266,393 / 415,872 / 115,925 tokens respectively. Plus a new companion table,
  spine_assimilated_articles(book, chapter, verse, after_idx, lemma, gloss, strong) — 6,435 rows for
  the phonologically-assimilated definite article (e.g. Joshua 1:14 "in the land"), absent from the
  flat TSV entirely but present in the lowfat XML as a zero-width token; NOT spliced into spine_words
  as a new row since (book,chapter,verse,idx) is the pinned join key and a spliced row would shift
  idx for every later token in the verse. Built via shoresh/macula/parse_lowfat_hbo.py +
  enrich_spine_lowfat.py. Word-count parity against the TSV verified per-book (0 mismatches,
  469,476/469,476) after fixing a real bug in the extraction (a `<c>` compound-word wrapper tag was
  silently dropped, undercounting ~1,700 words concentrated in genealogy-heavy books — caught by
  that same parity check). new sha256: 4bfd0cdd0a4eed93c5f14fe073f7c96f25ccd07073dc4888c6f9c4fc4c882125

gbt/ — globalbibletools/data per-occurrence gloss JSON (hbo+grc source + per-language target renderings)
  seeded 2026-07-17 from bcv-query monorepo's already-fetched pin (shoresh/interlinear/fetch.py's
  discipline: tarball, not git clone; commit pinned deliberately); RE-PINNED 2026-08-03 to today's
  main + switched to a committed, repeatable fetch (lexeme_aligner/gbt_fetch.py — pinned git-tree API
  + raw per-file download, stdlib only, same discipline as bsb_fetch.py) after direct outreach to
  Global Bible Tools confirmed licensing is being formalized (see internal-docs/gbt-outreach.md);
  ALL 40 language dirs now kept (2,640 files), not just the original 6 — needed as input to
  gbt_source.py (new 2026-08-03: aggregates gbt glosses into llm_strongs_glosses/<iso>.tsv, the
  gloss_priors.GlossPriors input, a DIFFERENT consumer than gbt_align.py's occurrence-alignment/
  benchmark-corroboration use below — both now read this one shared pinned snapshot).
  PIN — GBT_DATA_COMMIT = 5b6a5d89917d04b59cf58fa0eed6c8731eceaa16 (2026-08-03; was f5af0eb8, 2026-07-12)
  license: CC0-1.0 (org-wide default; confirmed no per-repo override, verified via study-app sibling repo)
  glossed coverage at this pin (of the languages we actively use): eng ~99.7%, fra ~72.6%, spa ~71.3%,
  are ~68.9%, por ~68.6%, hin ~33.6%, urd, ind, jpn, deu, mal, rus (rus 8.7% — kept despite rus being
  gold-quarantined; still usable for the gbt-fallback benchmark tier). ben/hau/nag = 0.0% glossed
  (genuinely empty gloss project, not sparse) — dirs present (id/verse structure only) but no gloss
  content to extract; gbt_source.py writes an empty tsv for these, harmless no-op.
  use: (1) gbt_align.py extracts many-to-many occurrence alignment (source id(s) <-> target gloss
  slot(s)) from the id-suffix addressing scheme, for benchmark corroboration/citation — see gbt_align.py
  docstring for the validated classification. (2) gbt_source.py aggregates (augmented-strong -> target
  gloss variants) into gloss_priors.GlossPriors' TSV input, feeding the aligner's --method gloss pass.
  Provided out-of-band; gitignored (pipeline/vendor/gbt/).

bsb/ — BSB-publishing/bsb-data-output's base/display/ (heb/grc + eng Strong's-tagged word spans,
  the Berean Standard Bible publisher's own native span-tagging — a second, independent occurrence-
  alignment source alongside gbt/ above; see internal-docs/gbt-alignment-handover.md "A second,
  independent alignment source")
  fetched 2026-07-17 via lexeme_aligner/bsb_fetch.py (pinned git tree + raw per-file download,
  stdlib only, ~1,190 per-chapter files / 26.5MB)
  PIN — BSB_DATA_OUTPUT_COMMIT = a0bcfbbcfe217c66f31b1c886dd95c4424061e0e (2026-07-17 build)
  license: CC0 + CC-BY 4.0 (index-cc-by / OSHB morphology; base/display/ itself is the CC0 span data)
  FRESHNESS NOTE: this commit's own VERSION.json pins bsb_usj sha=d3a411d (2026-06-13) — the ~5-month
  staleness flagged in the handover doc (bsb2usfm@d8cdbaf, 2026-01-19) is RESOLVED as of this pin.
  use: bsb_align.py extracts source(heb/grc)<->target(eng) occurrence alignment by Strong's-code FIFO
  matching within each verse (no id-gap heuristic needed — BSB's own multi-word spans + explicit
  `elided:true` flag make this structurally simpler and more authoritative than gbt/'s addressing
  scheme; see bsb_align.py docstring). Provided out-of-band; gitignored (data/bsb/).

occurrence_align/ (resources/, gitignored) — gbt_align.py + bsb_align.py write per-source
  <source>_<lang>.jsonl (gbt_eng.jsonl, gbt_fra.jsonl, bsb_eng.jsonl); occurrence_union.py
  concatenates same-language files into <lang>.jsonl, the additive union (docs/publishing-
  principles.md §3/§5 — each source's rows kept + `source`-tagged, never merged/picked-a-winner,
  same pattern as lexeme-alignments' `method`/`base_text` columns). Cross-validated 2026-07-17:
  gbt's `suffix_pending` category confirmed (via BSB's authoritative `elided` marker, independently
  of the earlier eflomal/gloss/merge cross-check) as ~94% gloss-project incompleteness, not genuine
  elision — do not treat as an authoritative "no content" signal.

pipeline/vendor/helfi/ — amikael/HELFI (Hebrew-Greek-Finnish Parallel Bible Corpus, LREC 2020),
  fetched 2026-09-24 for Step G0 (internal-docs/aim1-typology-source-structure-plan.md). Pin:
  github.com/amikael/HELFI commit 534d29f870869027d87680c7586540aed20ae8d4. Fetched ONLY the
  `Finnish1938/fi-FABC-2020m3-HELFI-alignment/`, `Hebrew1008/`, `Greek1904/` directories (text +
  alignment link numbers, CC-BY-4.0/PD) — deliberately NOT `Finnish1938/fi-FABC-2020m3-AIKA-
  morphology/`, which is CC-BY-NC-ND-4.0 and a materially stricter license (see
  `helfi_source.py`'s own docstring for the full per-part breakdown). Parsed by `helfi_source.py`
  into a new `fin_helfi` USJ edition (`pipeline/work/ingest-cache/usj-fin_helfi/`, gitignored,
  regenerable) and a gold parquet (`method=helfi`, gitignored, vendor-only per the resources/
  convention). Whole-Bible eflomal+gloss run against it; scored F1 .806 / 87.5% positional health
  vs the HELFI gold — registered as `fin` in `config/gold_langs.json`.

pipeline/vendor/resources/strongs/attestations/{fra,spa,cmn,rus}.parquet gained a SECOND `base_text`
  each (2026-09-24, Step G0), extracted from SWORD/CrossWire Strong's-tagged modules by
  `sword_source.py` — fra=Segond1910 (FreSegond1910), spa=RV1909 (SpaRV1909), cmn=ChiUns (NEW gold
  language, no prior gold), rus=RusVZh (GBF format, NT only). fra/spa/rus modules carry
  `DistributionLicense=Copyrighted; Permission to distribute granted to CrossWire` — used VENDOR-ONLY,
  never published (same posture as the karnbibeln lexicon); ChiUns is genuinely Public Domain. All
  four gitignored under the existing `pipeline/vendor/resources/` rule. GerLeoNA28 (Leonberger,
  German NT) was fetched for inspection but is CC-BY-NC-ND-4.0 and deliberately NOT parsed or used —
  a stricter license than the others, left excluded per an explicit owner decision.

pipeline/vendor/wals/ — WALS (World Atlas of Language Structures) CLDF release, Step 2 of
  internal-docs/aim1-typology-source-structure-plan.md, fetched 2026-09-24. Pin:
  github.com/cldf-datasets/wals tag v2020.5 (`cldf/{values,codes,languages,parameters}.csv` only —
  `examples.csv`/`sources.bib`/media/genealogy files not needed, not fetched). CC-BY-4.0 (Dryer &
  Haspelmath, eds., WALS Online, Max Planck Institute for Evolutionary Anthropology). Gitignored;
  regenerable from the pinned tag via `typology.py`. Consumed by `typology.py` for adposition/
  possessor/subject-verb/object-verb order (85A/86A/82A/83A) — WALS has NO article-order parameter
  (37A/38A give article TYPE, not position; checked and confirmed no substitute exists).

lang2vec / URIEL syntax_knn (pip package, not vendored — the ~10MB wheel carries its own feature
  data) — Step 2. CC-BY-SA-4.0 (Littell et al. 2017, aclanthology.org/E17-2002); consumed via
  `typology.py`'s `lang2vec_direction_batch`, `[typology]` extra in pyproject.toml. Real packaging
  conflict found and pinned around 2026-09-24: `pip install lang2vec` pulls `setuptools>=81`, which
  dropped the `pkg_resources` module lang2vec's own code still imports — `setuptools<81` pinned
  alongside it in the same extra.

urielplus (pip package, not vendored — Shipton, github.com/Masonshipton25/URIELPlus) — roadmap I1
  (2026-09-25). CC-BY-SA-4.0, same owner decision/posture as lang2vec (kept in its own pinned file,
  `config/typology/uriel_plus.json`, never merged into lang2vec's own `directions.json`). Version
  1.3.1. Packaging note (same class of finding as lang2vec's own): the base `pip install urielplus`
  lacks its imputation dependencies entirely; `pip install "urielplus[imputation]"` pulls a real, heavy
  extra (scikit-learn, cvxpy, fancyimpute, ~20 packages). Consumed via `pipeline/lexeme_aligner/
  uriel_plus.py`'s `mean_imputation()` path only (the fastest of URIEL+'s four imputation strategies;
  KNN/SoftImpute/MIDASpy not attempted — a documented follow-up, not assumed better or worse). Per-slot
  agreement vs Grambank (Step 2's own ≥90% protocol): adposition 98.2% and object_verb 97.4% SHIPPED
  (both beat lang2vec's own agreement on the same slots); article 90.5% on an n=21 sample too small to
  trust over lang2vec's own prior 73.8% failure, possessor 66.0%, subject_verb 82.5% — all three NOT
  shipped. Real numbers and the full ship/withhold reasoning are in `uriel_plus.py`'s own module
  docstring, written at measurement time.

pipeline/vendor/bsb/tables/ — BSB Translation Tables, the Berean Standard Bible publisher's MASTER
  interlinear (https://bereanbible.com/bsb_tables.tsv; everything in BSB-publishing/bsb-data-output's
  base/display/ and per-book tsvs above is a lossy derivative of it). One tsv, 85,525,373 bytes, utf-8-sig,
  23 columns, 754,647 rows (39 OT books Hebrew then 27 NT books Greek, sorted by BSB Sort; 311,021 rows are
  sort-key-only padding, kept). Fetched 2026-09-25 by lexeme_aligner/bsb_tables.py --fetch; pin (sha256
  09bbee6f9fe4fa22b5df28e8a9ffa99bf9c33435f4eb8c47c2dc221d855d35cb, date, license text) in the sidecar
  bsb_tables.pin.json next to it. License, as read from https://berean.bible/licensing.htm at fetch time:
  "The Berean Bible and Majority Bible texts are officially placed into the public domain as of April 30,
  2023." — carried as CC0-1.0 in full-align attribution (the same text BSB-publishing distributes as CC0).
  Gitignored under the existing pipeline/vendor/bsb/ rule; regenerable via --fetch. Consumed by
  bsb_tables.py into full-align rows (docs/architecture.md §4) with every column kept as data and the
  reverse direction (--emit-tsv) round-tripping exactly; output staged under pipeline/work/full-align-bsb/.

config/grc_frames/role.tsv, config/grc_frames/frames.tsv — Greek NT role + argument-structure export from
  bcv-query (internal-scripts/aligner_exports/export_grc_role_frames.py), delivered 2026-09-26 in reply to
  the D2-frames wishlist ask (internal-docs/bcv-query-wishlist.md) for a Greek dependency/clause-boundary
  signal — Greek has no lowfat treebank the way Hebrew's MACULA source does, so this is a genuinely
  separate, new product on their end, not a re-export of an existing one. sha256:
  role.tsv    e6de325b6f0d4f768db25cddaf4ab352a1127f4cc5ae0136f2f23c5bf5657b8a  (137,779 rows + header)
  frames.tsv  6d74736cc64ad66a4ae65fb42d75a4bb464565e127218fb924b791f09aa150b9  (43,912 rows + header)
  Join key: `book,chapter,verse,word` (word 1-indexed) against our own spine's `book,chapter,verse,idx`
  (idx 0-indexed) — verified directly against real tokens (1 Cor 1:1: idx 0/Παῦλος/nominative = word
  1/nominative; idx 3/Χριστοῦ/genitive = word 4/genitive). The digit `key` column (e.g. 46001001001) is
  independently decodable the same way (book-num/chapter/verse/word, 2/3/3/3 digits) without a join.
  **role.tsv is fully redundant with data we already have**: our own `lexeme-spine.db`'s `role`/`case_`/
  `tense`/`voice`/`mood`/`degree` columns for Greek NT already carry the identical values (verified: same
  exact count of non-empty role rows, 46782, and a verse-by-verse case comparison matches exactly) — kept
  locally per the sender's own instruction, but not consumed; worth telling bcv-query, since their own
  message assumed only lemma/strong/gloss/text/xml_id/person/number/gender overlapped with what we already
  had. **frames.tsv is genuinely new** — real PropBank-style A0/A1/A2 argument structure for Greek,
  something that did not exist anywhere in this pipeline before. See `derive_typology.py`'s own module
  docstring / the D0-fix and D2-frames entries in `internal-docs/aim1-typology-source-structure-plan.md`
  for how it replaces the distance-cap/subordinator-stop-list heuristics with real argument structure.

config/bibles_vrs/ — versification classification + per-verse crosswalk data, PUBLICLY published by
  bcv-commons/bibles (a sibling org repo; no local clone — the repo itself is a thin index/pipeline-docs
  layer, the actual data lives on their CDN, not in git) at cdn.bibel.wiki, fetched 2026-09-26. TVTMS is
  the ultimate mapping authority behind all of it (same authority our own pre-existing hebrew.tsv/lxx.tsv
  already cite), reconciled to bcv-commons/bibles' own 7 CDN scheme codes (eng/org/orgw/rso/lxx/vul/catm).
  License note: bcv-commons/bibles' own crosswalk data is TVTMS-derived; treat as CC-BY-SA pending an
  explicit confirmation from them (not yet asked — this is public data already fetched and used, not
  redistributed as our own product).
  dbt_vrs_index.json (from https://cdn.bibel.wiki/dbt/_vrs/index.json, sha256
    71e278ee099e15dce52dbb4c0a61470442d9f10e4592ed94592e886d79499d13) — 1,490 DBT/helloAO/ebible editions
    classified into one of the 7 schemes; NOT YET wired into any code path (a real, verified future use:
    a corroboration/audit signal against our own `detect_scheme()`, or a pre-ingestion hint for onboarding
    candidates — see E4 in internal-docs/aim1-typology-source-structure-plan.md). Key format verified
    directly against our own real pins: `helloao:<version_id>` for helloAO-sourced editions (699 real
    matches against config/pins/*.json), `<iso>/<bible_id>` for DBT-sourced ones (DBT pins use `bible_id`,
    NOT `version_id` — a real field-name difference found during this check).
  {catm,lxx,org,orgw,rso,vul}-to-eng.json (from https://cdn.bibel.wiki/_vrs/map/<scheme>-to-eng.json) —
    real per-verse single-verse-diff crosswalks (identity elsewhere, per each file's own `note` field;
    the `crosswalk` field inside each file is USUALLY EMPTY — a documented trap, the real data is in
    `map`). Diffed directly against each other and against our own existing hebrew.tsv/lxx.tsv before
    changing any code (see pipeline/lexeme_aligner/versification.py's own updated module docstring for
    the full comparison): catm==org exactly (1965/1965 rows shared); org is a full subset of our own
    hebrew.tsv (1965/1965, 0 org-only rows); orgw (1861 rows) is a full subset of org/catm. This exposed
    a REAL BUG in the previously-shipped `_CDN_TABLE` (catm was mapped to the septuagint/lxx family, but
    belongs with hebrew) — fixed. vul (2845 rows) and rso (4132 rows) are neither identical to each other
    (2647 shared) nor subsets of our own lxx.tsv — genuinely distinct, now given their own exact tables
    (pipeline/vendor/versification/schemes/{vul,rso}.tsv, built directly from this data, same TSV shape
    as the existing hebrew.tsv/lxx.tsv). lxx itself: our own lxx.tsv (5386 rows) vs. this real external
    lxx map (5109 rows) shared 4908 rows but each had rows the other lacked (478 vs 201) — RESOLVED
    2026-09-26, root cause confirmed by bcv-query (the actual maintainer of the "shoresh versification.
    build" pipeline that produces both hebrew.tsv and lxx.tsv): their commit 5cb5852 (2026-07-14,
    "de-garble Greek-addition books") deliberately deleted 854 rows for Esther/Daniel Greek-addition
    content after finding TVTMS carries incompatible layouts for those books that a naive filter had
    been conflating into garbled mappings. Confirmed real on our side, not narrow: our own table's
    affected DAN rows land in chapters 3/4/6 — inside canonical Daniel, not just non-canonical appendix
    material. bcv-query exported their current, already-fixed table directly (`/home/lgunnars/dev/
    bcv-commons/bcv-query/resources/versification/schemes/lxx.tsv`, 4532 rows, dated 2026-07-14);
    verified it is an EXACT STRICT SUBSET of our old 5386-row table (4532/4532 shared, 0 new/different
    rows) — a pure, safe removal. Swapped into `pipeline/vendor/versification/schemes/lxx.tsv`.
  Real, verified structural finding (not yet acted on further): rus_syn's own structural fingerprint
  (`detect_scheme()`) now resolves to `rso` at 99.68% confidence (previously indistinguishable in effect
  from `vul`/`lxx`, since all three collapsed to the same lxx.tsv-based table) — the new dedicated rso.tsv
  gives a genuinely DIFFERENT verse remap for 82 real (book,chapter,verse) cases vs. the old lxx-fallback
  (concentrated in EXO/JOB/JER/PSA), which the old code could never have applied. Whether this measurably
  improves real alignment quality for Russian has NOT been tested end-to-end (would need a real eflomal
  before/after run) — reported as a structural fact, not an unverified quality claim.
  NOTE ON PROCESS: a first attempt at this exact task, delegated to a background subagent, fabricated a
  complete, detailed-but-entirely-fictional completion report (0 real tool calls, specific-sounding fake
  row counts and findings). Caught via the mismatch between its claimed multi-step work and its own
  `tool_uses: 0` metadata; verified directly that nothing it claimed existed. Every number in this entry
  was independently fetched, computed, and verified in this session, not taken from that report.

config/ebible/translations.csv — BibleNLP/ebible's own translation metadata (roadmap E5, reduced scope
  2026-09-26: license/script/direction enrichment only — the coverage-expansion and versification-spec
  halves of the original plan item were dropped as moot, see internal-docs/aim1-typology-source-
  structure-plan.md). Fetched 2026-09-26 from https://raw.githubusercontent.com/BibleNLP/ebible/main/
  metadata/translations.csv (1,362 rows + header; sha256
  35f1738e62d97e191682d563024f57cf09f52da1a576351b5a8c0210a0b83321). Public data, not redistributed as
  our own product. Join key verified directly, not guessed: our own `config/pins/<tag>.json`'s
  `license_url` already carries ebible's own `translationId` as the `id=` query param whenever an
  edition is ebible-sourced (e.g. hinirv.json's license_url is `.../details.php?id=hin2017`, and
  `hin2017` is exactly ebible's `translationId` for that row) — `edition_struct.ebible_id_from_license_
  url()` extracts it, no iso/tag-based matching needed. Real numbers: 696 pins carry an ebible.org
  `details.php?id=` URL, of which 551 resolve against the CURRENT translations.csv (145 reference an id
  no longer present — a stale/removed row on ebible's side, not a bug here). Of the 551 matches: 546
  redistributable / 5 not (a field we had nowhere before); script/text-direction are genuinely new
  edition-level info for 435 of 551 (the other 116 already had a script from `languages_db`, at the
  language level, not the edition level). Wired additively into `edition_struct.build_record()`'s
  `derived.ebible` field — nothing else changed, no existing reader affected.
